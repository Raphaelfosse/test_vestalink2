<% @sites.each do |site| %>
  <div class="card">
    <div class="card-header">
      <%= site.address_line1 %></br>
      <%= site.city %>
      <%= site.zipcode %>
    </div>
    <div class="card-body">
      <blockquote class="blockquote mb-0">
        <p><%= site.reference_site %></p>
        <p><%= site.nom %></p>
        <footer><%= link_to "Lots associés", site_parts_path(:site_id => site), style: "color:#000000" %></br><cite title="Source Title"><%= link_to "Modifier ce site", edit_site_path(site), style: "color:#000000" %></br><%= link_to 'Voir ce site', site_path(site) %></cite></footer>
      </blockquote>
    </div>
  </div>
<% end %>
